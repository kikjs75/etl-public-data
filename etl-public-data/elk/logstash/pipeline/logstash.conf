input {
  beats {
    port => 5044
  }
}

filter {
  # message 필드에 담긴 JSON 파싱 (파싱 실패 시 원본 유지)
  json {
    source => "message"
    skip_on_invalid_json => true
  }
}

output {
  stdout {
    codec => rubydebug
  }
}
